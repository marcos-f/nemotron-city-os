fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
PyYAML>=6.0
httpx>=0.27
# Not on PyPI — obtained by scripts/fetch-throughline.sh from the wheel
# throughline's `package` job publishes. Install it before this file.
throughline>=0.2
