# breaker — Product Roadmap (scoped)

## v0.1.0 — Spec (this MR)
Feature-area + use-case spec (UC-002), scoped idea (IDEA-001 Grid Watch),
sitemap + interaction-graph slice, breaker.html wireframe with element
coverage, architecture slice + registry row + tech-review slice, gate-01/02/03
run strict and green. SPEC-ONLY — no runtime code.

## v0.2.0 — Build (B2b)
Per `.viper-context/plan/web/v0.1.0/briefs/BUILD-GOAL-breaker.md`: spec-03
telemetry generator, deterministic divergence rule with line-by-line
evidence render, dispatch proposal (irreversible) into the throughline gate,
substrate contract with cuOpt REGISTERED alternate (unavailable-without-GPU;
never claim solver where a rule runs).

Acceptance carried from the brief: test://breaker/divergence-fires,
test://breaker/gate-holds-forever (no timeout branch in code),
test://breaker/approval-executes, test://breaker/ledger-walk-3hops.

## v0.3.0+ — Out of scope for this goal
Runtime hardening, key rotation follow-ups (tracked at orchestrator level),
any Spark/local-NIM path if team hardware materializes.
